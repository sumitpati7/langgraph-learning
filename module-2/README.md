# Module 2
Module 2 focuses on state and memory of the graph.

## Task Completed
- State Schema
  - State schema is representation of data shared between nodes.
  - Each node can view them and edit them.
  - state is passed automatically to the nodes.
  - `TypedDict`, `Dataclass` and `Pydantic` can be used to define state schema.